import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById('root'));

const e1 = (
  <>
    <h1 className="text-xl font-bold text-black">This is a react app</h1>
    
  </>
); 
root.render(e1);
